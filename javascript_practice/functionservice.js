const userService=(value)=>{
    return `Passed value to function service is ${value}`
}

const ageService=(age)=>{
    return `Passed age to function service is ${age}`
}

module.exports = {userService, ageService};
